version: 3

tasks:
  build:
    aliases:
      - b
    desc: generate api
    cmds:
      - protoc -I ./api ./api/gophkeep-grpc-api.proto --go_out=./pkg --go_opt=paths=source_relative --go-grpc_out=./pkg --go-grpc_opt=paths=source_relative